# 贡献指南

## 开发流程

1. 使用 README 中的工具链，从 `mihome-hook/` 构建。
2. 行为修改先编写失败测试，再实现；保留包/进程门控、反射预检和失败回退。
3. 运行 `:app:testDebugUnitTest :app:lint :app:assembleDebug :app:assembleRelease`。
4. PR 说明实际运行的验证、未验证范围、对旧结构的影响。不要用测试替身通过来宣称真机兼容。

## 真机回归清单

使用可安全测试的普通应用，保留桌面恢复手段。不要挂起系统桌面、SystemUI、管理工具或关键业务应用。

- 启用模块、只选系统桌面作用域并重启桌面后，确认日志出现 INSTALLED。
- 挂起后桌面、应用抽屉、两种文件夹预览保持彩色；真实启动限制仍存在。
- 解除挂起后应用可以正常启动，重复切换和重启桌面后状态正确。
- 工作资料暂停、混合禁用原因、安装/下载占位图标仍保留原效果。
- 回归其他图标模块共存、原滤镜为 null、未知主题图标及不兼容接口的回退。
- 停用模块并重启桌面可以恢复原始显示，不需要清除桌面数据。

## 隐私和提交范围

提交前检查 `git diff --cached`，不能仅依赖 .gitignore。不得提交 APK、反编译树、设备备份、日志、账号/邮箱、绝对用户目录、序列号、签名密钥、访问令牌或工作资料信息。公开仓库账号和通用示例包名不属于需要隐藏的构建凭据。

问题报告可包含 Android/桌面/LSPosed 版本与脱敏后的固定模块事件。不要直接上传完整 logcat、桌面数据库、私人图标截图或未经授权分发的厂商 APK。

提交即表示你有权贡献该内容，并同意按项目 Apache-2.0 许可证分发；第三方内容应明确注明来源和许可。
